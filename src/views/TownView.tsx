import React from "react";

interface TownViewProps {}

const TownView = (props: TownViewProps) => {
  return (
    <div>
      <h1>TownView component rendered</h1>
    </div>
  )
};

export default TownView;
